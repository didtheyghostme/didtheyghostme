type Note = {
  id?: number; // Optional for new entries
  title: string;
};

type Company = {
  id?: number; // Optional for new entries
  company_name: string;
  company_url: string;
  status?: string;
  user_id?: string;
};
